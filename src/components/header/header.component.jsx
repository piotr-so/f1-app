import React from 'react';
import { AppHeader, Logo } from './header.styled';

const Header = () => {
    return (
        <AppHeader>
            <Logo />
        </AppHeader>
    );
}

export default Header;
